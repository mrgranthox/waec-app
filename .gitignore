# Build artifacts
target/
**/target/
build/
.dart_tool/
.flutter-plugins*
*.lock
!mobile/pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Secrets — never commit (plan §0.8)
.env
*.pem
!microservices/common/tests/fixtures/*.pem
*.key
secrets/

# Flutter
.packages
.pub-cache/
.pub/

doc/WAEC Result Verification App/

# Logs
*.log

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
infra/compose/.env

# Release debug symbols (plan §4.2). These decode an obfuscated binary back
# into readable names, so they belong in the release vault, never in git.
build-symbols/
