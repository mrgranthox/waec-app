-- 0006_checkers.sql — checker vault + result snapshots (plan §2.7 / §3.7).
-- Managed by the `waec-data` crate's inline DDL (see checkers.rs), not by
-- versioned files. This stub exists for documentation + drift detection.

-- The real DDL lives in microservices/data/src/checkers.rs::run_checker_migrations.
-- Keeping this file in sync is a CI smoke check, not the migration mechanism.
