# Mock WAEC portal fixtures (dev-only, plan task 1.5).
# Deterministic HTML for parser fixture tests — mirrors the real DOM
# schema recorded from eresults.waecgh.org / ghana.waecdirect.org.
# DOM-drift tests change the structure here and assert the parser aborts.
